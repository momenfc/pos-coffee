import { css } from '@emotion/css';
import { Layout } from 'antd';

const { Footer } = Layout;

function OwnFooter() {
  const OwnFooterStyles = css``;

  return <Footer className={OwnFooterStyles}>OwnFooter</Footer>;
}

export default OwnFooter;
